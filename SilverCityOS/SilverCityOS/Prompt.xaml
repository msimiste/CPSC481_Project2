<Window x:Class="SilverCityOS.Prompt"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SilverCityOS"
        mc:Ignorable="d"
        Title="Prompt" Height="500" Width="800" WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True">
    <Grid>
        <Border BorderBrush="Black" BorderThickness="1 1 1 1">
            <Grid Background="#FFF">
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel x:Name="mainStack" Grid.ColumnSpan="2" VerticalAlignment="Center">
                    <TextBlock x:Name="message" VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="80 50 80 0" TextAlignment="Center" FontSize="40" Padding="30 0" TextWrapping="Wrap">Waiter Has Been Notified</TextBlock>
                    <Border x:Name="itemBorder" BorderBrush="Black" BorderThickness="2" Margin="80 10 80 0">
                        <ScrollViewer Height="200" BorderBrush="Black" BorderThickness="2">
                            <StackPanel x:Name="itemList">
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </StackPanel>
                <Button Grid.Column="0" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="90 30 30 60" Padding="10 10" Click="Button_Click_Okay">
                    <Viewbox>
                        <Label>
                            OKAY
                        </Label>
                    </Viewbox>
                </Button>
                <Button Grid.Column="1" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="30 30 90 60" Padding="10 10" IsCancel="True">
                    <Viewbox>
                        <Label>
                            CANCEL
                        </Label>
                    </Viewbox>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>
